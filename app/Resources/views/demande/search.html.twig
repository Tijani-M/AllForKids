{% extends "dashboard.html.twig" %}

{% block body %}

    <div class="x_panel">
        <div class="x_title">
            <h2>Adresse
                <small>recherche</small>
            </h2>
            <ul class="nav navbar-right ">
                <li>
                    <a href="{{ path('demande_new') }}">Ajouter une demande</a>
                </li>
            </ul>
            <div align="center">
                <form action="search" method="post">
                    {{ form_start(form) }}
                    {{ form_widget(form.adresse, {
                        'attr': {'class':'dropdown-toggle'}
                    }) }}

                    <input class="btn btn-primary" type="submit" value="Rechercher"/>
                    {{ form_end(form) }}
                </form>
            </div>

            <div class="clearfix"></div>

        </div>

        <div class="x_content">
            <table id="datatable" class="table table-striped table-bordered">
                <thead>
                <tr>
                    <th></th>
                    <th>Nom</th>
                    <th>Prenom</th>
                    <th>adresse</th>
                    <th>Tel</th>

                </tr>
                </thead>
                <tbody>
                {% for adr in adresses %}
                    <tr>
                        <td align="center">
                            <a href="{{ path('demande_show', { 'id': demande.id }) }}"><i class="fa fa-eye"></i></a>
                        </td>
                        <td>{{ demande.nom }}</td>
                        <td>{{ demande.prenom }}</td>
                        <td>{{ demande.adresse}}</td>
                        <td>{{ demande.Tel }}</td>
                        <td>
                            <a href="{{ path('demande_edit', { 'id': demande.id }) }}">modifier/supprimer</a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
