{% extends 'base.html.twig' %}

{% block body %}
    <h1>Typepara</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typePara.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ typePara.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('typepara_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('typepara_edit', { 'id': typePara.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
