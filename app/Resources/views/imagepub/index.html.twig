{% extends "dashboard.html.twig" %}

{% block body %}

<div class="title_left">
    <h1 align="center">Publication Des Images</h1>
</div>

<div class="x_panel" align="auto">
    <div class="x_title">
        <div class="clearfix">
            <ul class="nav navbar-right ">
                <a href="{{ path('imagepub_new') }}"><i class="fa fa-save"></i> Create a new image</a>
            </ul>



            <div class="x_content">
                <table id="datatable" class="table table-striped table-bordered">
                    <thead>

              <tr>
                <th>Id</th>
                <th>Description</th>
                <th>Imagename</th>
                <th>Updatedat</th>
                <th>Actions</th>
            </tr>
        </thead>

        <tbody>
        {% for imagePub in imagePubs %}
            <tr>
                <td><a href="{{ path('imagepub_show', { 'id': imagePub.id }) }}">{{ imagePub.id }}</a></td>
                <td>{{ imagePub.description }}</td>
                <td>{{ imagePub.imageName }}</td>
                <td>{% if imagePub.updatedAt %}{{ imagePub.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('imagepub_show', { 'id': imagePub.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('imagepub_edit', { 'id': imagePub.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

</div>
{% endblock %}
